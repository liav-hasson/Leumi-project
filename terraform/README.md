task:
Terraform section:
a. Write a tf file that will create a machine identical to the Linux-based EC2 test, the machine must run Apache on port 80, http protocol, contain a fixed cyber address vip, in addition to sg that will only allow the address of Leumi Proxy to browse to it (91.231.246.50).
b. Make the machine from section a accessible by nlb.
Use Cloudfront.